# Largest-Rectangle-in-Histogram
Given an array of bar heights (width = 1), find the maximum area rectangle that can be formed within the histogram. Solved in O(N) time using a monotonic stack to track increasing heights and calculate areas when a smaller height is encountered.
